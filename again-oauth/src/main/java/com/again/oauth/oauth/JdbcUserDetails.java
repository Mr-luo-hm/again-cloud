package com.again.oauth.oauth;

/**
 * @author create by 罗英杰 on 2021/8/18
 * @description:
 */
/*
 * public class JdbcUserDetails implements UserDetailsService {
 *
 *
 * @Override public UserDetails loadUserByUsername(String username) throws
 * UsernameNotFoundException {
 *
 *//*
	 * Credentials credentials = credentialsDao.findByName(username); if (credentials ==
	 * null) { throw new UsernameNotFoundException("User '" + username +
	 * "' can not be found"); }
	 *//*
		 *
		 * return new User(credentials.getName(), credentials.getPassword(),
		 * credentials.isEnabled(), true, true, true,
		 * credentials.getGrantedAuthorities()); } }
		 */
